syntax = "proto3";

message Simple {
    uint32 id = 1;
    bool is_simple = 2;
    string name = 3;
    repeated int32 sample_lists = 4;
}

// decode raw data command => cat simple.bin | protoc --decode_raw